(setq major-mode 'runekichi-mode)
(setq mode-name "runekichi mode")

(setq rune-map(make-keymap))
(define-key rune-map "[a-z]" 'RuneEmon)

(defun RuneEmon()
  (interactive)
  (insert "ぼくるねえもん"))

(use-local-map rune-map)
