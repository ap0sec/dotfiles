@extends('layouts.prichanEx')

@section('title','card')
@section('page','card')

@section('contents')
    <div class="container">
        <form class="form-inline">
            <div class="form-group mb-2">
                <input type="text" readonly class="form-control-plaintext" id="statictarget" value="Search">
            </div>
            <div class="form-group mx-sm-3 mb-2">
                <input type="text" class="form-control" id="inputtarget" placeholder="">
            </div>
            <button type="submit" class="btn btn-primary mb-2">Search</button>
        </form>

        
        <div class="table-responsive">
            <table class="table table-striped table-bordered">
                <tr><th>card id</th><th>rarity</th><th>brand id</th><th>name</th></tr>
                @foreach ($items as $item)
                    <tr>
                        <td>{{$item->cardid}}</td>
                        <td>{{$item->rarity}}</td>
                        <td>{{$item->brandid}}</td>
                        <td>{{$item->cardname}}</td>
                    </tr>
                @endforeach
            </table>
        </div>
    </div>
@endsection

@section('footer')
    copyright 2018 ap0sec.
@endsection
