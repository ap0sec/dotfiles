#!env python
import sys

readsize = 1320
loop = 32

with open("1.jpg","rb") as f:
    infile = f.read(readsize)


    
input = infile.hex()
output = []
for i in range(loop):
    output.append(input[:5*(i+1)])
    input = input[5*(i+1):]

outfile = ''
for i in output:
    outfile += str(i.count("0")) + ","

print(outfile)
